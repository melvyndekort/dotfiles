#!/bin/sh

choice="$(rofi -dmenu -p 'SSH configuration')" << END
home
remote-open
remote-strict
END

if [ -n "$choice" ]; then
  cd $HOME/.ssh/
  ln -sf "config-${choice}" config && notify-send "SSH configuration set to $choice"
fi
